spec_version: '1.0'
component: PageHeader

# References only. Values live in @acronis-platform/tokens-pd. Only the muted
# breadcrumb/description carry color; the title inherits the foreground.
source: '@acronis-platform/tokens-pd'

tokens:
  - name: --ui-text-on-surface-secondary
    affects: page-header-description
    description: Muted breadcrumb + description text (text-muted-foreground).
